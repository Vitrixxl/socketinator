{
  "name": "socketinator",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "type": "module",
  "scripts": {
    "types:generates": "rm -rf ./packages/sdk/contracts && rm -rf ./packages/server/contracts && cp -r ./contracts ./packages/sdk && cp -r ./contracts ./packages/server ",
    "build": "bun run --filter '@socketinator/*' build"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "zod": "^4.1.12"
  }
}
